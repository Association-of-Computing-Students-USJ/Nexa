# Disabled — NEXA deploys to Firebase Hosting (see firebase-hosting.yml).
# Re-enable only if you intentionally want GitHub Pages instead.

name: Deploy (GitHub Pages) — disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use firebase-hosting.yml for production deploys."
